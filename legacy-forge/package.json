{
  "name": "legacy-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Flotte d'agents IA qui modernise du code .NET Framework vers .NET moderne : pipeline open source, observable, évalué, sous budget.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "forge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "forge": "node --enable-source-maps dist/index.js",
    "scout": "npm run build && node dist/index.js scout",
    "run:one": "npm run build && node dist/index.js run",
    "bench": "npm run build && node dist/index.js bench",
    "report": "npm run build && node dist/index.js report"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.260",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
